#ifndef MINE_ENUMS_HPP
#define MINE_ENUMS_HPP

namespace mine {
enum limits {
    NEG_X_BOUNDS,
    POS_X_BOUNDS,
    NEG_Z_BOUNDS,
    POS_Z_BOUNDS,
    NEG_X_AXIS,
    POS_X_AXIS,
    NEG_Z_AXIS,
    POS_Z_AXIS
};
}

#endif
